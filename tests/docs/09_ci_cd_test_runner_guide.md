# Continuous Integration & Automated Test Runner

## Running the Complete Test Suite
```bash
# Backend pytest suite (211 tests)
cd backend && pytest

# Frontend vitest suite (31 tests)
cd frontend && npm run test
```
